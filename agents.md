# Agent Instructions

This repository is a Python data pipeline for UFC fight ingestion, time-safe Elo snapshots, pre-fight feature generation, rolling fight-stat features, model evaluation, and probability calibration.

## Pre-task instructions

- Before performing any task, read the repository's applicable Markdown instruction files, including `agents.md` and `CLAUDE.md`, and follow their current guidance.

## Setup

- Use `python3`; `python` may not exist on all machines.
- A project virtualenv lives at `.venv/` (created with `python3 -m venv --without-pip .venv` plus get-pip.py on machines whose system Python lacks `ensurepip`). Prefer `.venv/bin/python` when it exists.
- Install runtime/test dependencies in a virtual environment when needed:
  `python3 -m pip install pandas numpy scikit-learn joblib pytest`
- There is currently no `pyproject.toml` or requirements file, so do not assume packaging metadata exists.

## Common Commands

- Run tests: `python3 -m pytest tests/ -q`
- Register fighter identities (run BEFORE ingesting fights so same-name fighters split): `python3 scripts/build_fighter_identities.py --tott data/raw/greco/ufc_fighter_tott.csv --db data/ufc.db --report data/processed/fighter_identity_report.json`
- Ingest fights: `python3 scripts/ingest_fights.py --input data/raw/mdabbert_ufc.csv --db data/ufc.db --source mdabbert`
- Build Elo: `python3 scripts/build_elo.py --db data/ufc.db`
- Export Elo baseline: `python3 scripts/export_elo_dataset.py --db data/ufc.db --output data/processed/ufc_elo_baseline.csv --ratings-output data/processed/current_fighter_elos.csv`
- Build Step 3 features: `python3 scripts/build_prefight_features.py --db data/ufc.db --output data/processed/ufc_prefight_features.csv`
- Build Step 3B features: `python3 scripts/build_prefight_features_step3b.py --db data/ufc.db --output data/processed/ufc_prefight_features_step3b.csv`
- Build Step 3C style-matchup features: `python3 scripts/build_prefight_features_step3c.py --db data/ufc.db --output data/processed/ufc_prefight_features_step3c.csv`
- Compare with Step 3C (versioned outputs, never overwrite the 3B comparison): `python3 scripts/compare_models.py --input data/processed/ufc_prefight_features_step3c.csv --metrics-output data/processed/model_comparison_step3c.json --predictions-output data/processed/model_comparison_step3c_predictions.csv`
- Calibrate Step 3C (writes step3c_-prefixed outputs): `python3 scripts/calibrate_model.py --input data/processed/ufc_prefight_features_step3c.csv --output-dir data/processed --model-dir data/models --feature-set step3c`
- Train/evaluate models: `python3 scripts/train_model.py --input data/processed/ufc_prefight_features.csv`
- Compare Step 3 vs Step 3B: `python3 scripts/compare_models.py --input data/processed/ufc_prefight_features_step3b.csv`
- Calibrate the best Step 3B model, backtest mode (Platt + isotonic, honest held-out test): `python3 scripts/calibrate_model.py --input data/processed/ufc_prefight_features_step3b.csv --output-dir data/processed --model-dir data/models` (optional `--train-frac/--calibration-frac` or `--train-end-date/--calibration-end-date`)
- Rolling/live calibration for future predictions (refits Platt on the most recent window; diagnostics only, no held-out test): `python3 scripts/calibrate_model.py --input data/processed/ufc_prefight_features_step3b.csv --output-dir data/processed --model-dir data/models --mode live --calibration-window-days 730 --method platt` (optional `--as-of-date`, `--min-calibration-rows`)
- Step 5A feature diagnostics (feature/scaling audit, group ablations, coefficients, permutation importance, leakage checks; writes `reports/step5a_feature_diagnostics.{json,md}` and `reports/step5a_coefficients.csv`): `python3 scripts/run_step5a_feature_diagnostics.py` (optional `--input`, `--output-dir`, `--permutation-repeats`, `--skip-permutation`)
- Step 5B regularization + weight_class-pruning search (LR-only L1/L2/elastic-net sweep + weight_class cleanup configs; validation-gated selection then finalists scored once on the held-out test; writes `reports/step5b_regularization_search.{json,md}`, `reports/step5b_candidate_results.csv`, `reports/step5b_coefficients.csv`): `python3 scripts/run_step5b_regularization_search.py --output-dir reports` (~19 min full run; optional `--quick`, `--max-candidates`, `--skip-balanced`, `--random-seed`)
- Step 5B.1 regularization confirmation audit (four pre-declared LR candidates only, five pre-test folds, final-test re-report only; writes `reports/step5b1_regularization_confirmation.{json,md}`): `python3 scripts/run_step5b1_regularization_confirmation.py --output-dir reports`
- Step 5C division-interaction experiment (LR-only `weight_class × feature` interactions for common/current divisions selected from pre-test counts only; writes `reports/step5c_division_interactions.{json,md}`, `reports/step5c_candidate_results.csv`, `reports/step5c_interaction_coefficients.csv`): `python3 scripts/run_step5c_division_interactions.py --output-dir reports`
- Step 5D nonlinear-model + blend comparison (Run 4; re-fits the official LR anchor + historical Step 3C LR, runs a bounded sklearn `HistGradientBoostingClassifier` grid with fit-window-only early stopping, an optional already-installed XGBoost/LightGBM and a non-promotable random-forest sanity check, plus a validation-tuned LR+HGB blend; five Step 5B.1 pre-test folds; validation-only selection; final-test re-report for finalists only; writes `reports/step5d_nonlinear_models.{json,md}`, `reports/step5d_candidate_results.csv`, `reports/step5d_feature_importance.csv`, `reports/step5d_blend_results.csv`): `python3 scripts/run_step5d_nonlinear_models.py --output-dir reports` (~1 min full run; optional `--quick`, `--skip-slow`, `--skip-blend`, `--skip-optional-dependencies`, `--include-rf-sanity`, `--max-candidates`, `--random-seed`). No odds, prediction markets, neural networks, or new data sources.
- Step 5D.1 nonlinear sanity audit (Run 4.1; diagnostics-only audit of the Run 4 result — anchor/HGB reproduction, probability orientation, LR/HGB row-feature parity, calibration protocol with AUC-invariance, HGB native NaN handling, an expanded staged HGB search plus max_iter/early-stopping sub-study, early-stopping isolation, and blend stability; reuses Step 5D machinery; writes `reports/step5d1_nonlinear_sanity_audit.{json,md}`, `reports/step5d1_hgb_sensitivity_results.csv`, `reports/step5d1_calibration_audit.csv`): `python3 scripts/run_step5d1_nonlinear_sanity_audit.py --output-dir reports` (~2.5 min full run; optional `--quick`, `--skip-slow`, `--max-candidates`, `--random-seed`). Promotes nothing; never edits the benchmark file.
- Step 6A historical pseudo-live replay (event-by-event: for each past event, re-fit the official LR + Platt on strictly-earlier fights only, predict the event, reveal results, score; writes a replay ledger `reports/replay/step6a_pseudo_live_predictions.csv` plus `reports/step6a_pseudo_live_replay.{json,md}` and `reports/step6a_rolling_metrics.csv` with overall/rolling/by-year/by-event/by-confidence metrics, calibration buckets, drift/data-quality, and leakage checks): `python3 scripts/run_step6a_pseudo_live_replay.py --input data/processed/ufc_prefight_features_step3c.csv --output-dir reports --min-train-fights 3000 --calibration-fights 1282 --overwrite` (~40s full run; optional `--start-date`, `--end-date`, `--max-events`, `--output-ledger`, `--random-seed`). It is a pipeline-validation and calibration/drift tool, NOT a new model search: reuses the official config as-is, never retunes/replaces the model, never writes `data/processed/` or the benchmark file, and needs `--overwrite` to replace an existing ledger. Pseudo-live ≠ static backtest (single fit on the fixed 2023-11..2026-05 window) and ≠ true future live tracking (reuses already-built Step 3C features, so time-safety rests on the upstream pre-fight builders).
- Step 6B forward live prediction ledger (`ufc_pipeline/step6b_live_predictions.py`): the forward live-tracking layer. Three commands — generate predictions for an upcoming card (`python3 scripts/run_step6b_live_predictions.py --input upcoming_card_features.csv --ledger data/live/live_predictions.csv --output-dir reports/live`), resolve results (`python3 scripts/resolve_step6b_live_predictions.py --results completed_results.csv --ledger data/live/live_predictions.csv --output-dir reports/live`), and build the live report (`python3 scripts/run_step6b_live_model_report.py --ledger data/live/live_predictions.csv --output-dir reports/live`). Input CSV: `event_date,event_name,fighter_a,fighter_b` + the 43 Step 3C model feature columns, no result/target/odds columns (rejected). Reproduces the official model (base LR on official train, Platt on official calibration), appends `pending` rows to a durable append-only ledger at `data/live/live_predictions.csv` (deterministic `prediction_id`; duplicates rejected unless `--allow-duplicate-predictions`, pending replaced only with `--overwrite-existing-pending`), resolves by exact normalized (date, fighter-set) match, and warns the live sample is too noisy < 50 resolved / preliminary < 100. Unlike Step 6A it predicts fights BEFORE they occur. Never retunes/replaces the model, never writes `data/processed/` or the benchmark. No scraper is included; a future Step 6C could add a data-source updater/scraper (still no odds/markets).
- Step 6C upcoming-card feature builder (`ufc_pipeline/step6c_upcoming_feature_builder.py`): turns a simple matchup CSV (`event_date,event_name,fighter_a,fighter_b` + optional `weight_class`) into the model-ready Step 3C feature CSV Step 6B expects. `python3 scripts/build_step6c_upcoming_features.py --matchups data/live/upcoming_card_matchups.csv --output data/live/upcoming_card_features.csv --validate-for-step6b --overwrite` (optional `--history-input` default `data/ufc.db`, `--event-date`, `--review-matches-output`, `--run-predictions` to chain into Step 6B). It is a direct recomputation (not a lossy bridge): appends each upcoming bout to the historical fights from `data/ufc.db` as a synthetic result-less fight and runs the exact Step 3/3B/3C builders with Elo from the deterministic engine over strictly-earlier fights — reproduces the processed file to |Δ|=0.0. Deterministic accent/punct-normalized exact name matching; unmatched/ambiguous names become reported failed rows (never guessed), and a fighter may appear in only one matchup per event date; debut/low-history fighters get training's null policy + `no_prior_stats` flags. Writes `data/live/` + a build report to `reports/live/step6c_feature_build_<ts>.{json,md}`, validates output against Step 6B and refuses to write an invalid file. No scraping, no odds/markets; never writes `data/processed/` or the benchmark. Feed the output straight into Step 6B.
- Step 6D.1 UFCStats data-update audit (`ufc_pipeline/step6d_ufcstats_update_audit.py`): READ-ONLY / dry-run audit that checks UFCStats against `data/ufc.db` and reports missing/stale events, fights, fighters, and results (so a human can decide whether the Step 6C history DB needs refreshing). `python3 scripts/run_step6d1_ufcstats_audit.py --db data/ufc.db --output-dir reports/data_update --max-events 10 --cache-dir data/cache/ufcstats --offline-cache-only` (optional `--include-upcoming`, `--start-date`, `--end-date`, `--delay`). It NEVER writes the DB (opens SQLite `mode=ro`). Stdlib-only (`urllib`+`html.parser`, no new deps), polite delay + max-events cap, on-disk HTML cache. Matching prefers stable UFCStats fighter URLs stored in `fighter_source_ids`, falls back to exact normalized names, and reports ambiguities instead of guessing. UFCStats serves a JS browser-challenge to plain HTTP clients; the fetcher detects it and degrades gracefully (recommends `--cache-dir` of HTML saved from a real browser + `--offline-cache-only`), never bypassing it. Reports go to `reports/data_update/step6d1_ufcstats_audit.{json,md}`. No odds/markets.
- Step 6D.2 guarded cached-event apply (`ufc_pipeline/step6d2_guarded_db_apply.py`): plans or explicitly applies one browser-saved completed UFCStats event. `python3 scripts/apply_step6d2_cached_event_update.py --db data/ufc.db --event-cache data/cache/ufcstats/event_<id>.html --output-dir reports/data_update` is read-only; `--apply` creates a timestamped DB backup, inserts only new rows, blocks ambiguous identities/non-append history, and can run a Step 6C smoke verification. It never scrapes, updates, or deletes existing DB rows, and never writes `data/processed/` or the benchmark.
- Step 6D.1b real-cached-HTML validation: the supported way to run a REAL audit (since the live JS challenge is not bypassed) is offline-cache mode over HTML you save from a normal browser (File > Save Page As > "Web Page, HTML only") into `data/cache/ufcstats/` (see the README there). Friendly filenames the audit accepts: `completed_events.html`, `upcoming_events.html`, `event_<id>.html` (id = the hash in the `event-details/<id>` URL); its own `sha1(url).html` files also work. Save the completed-events page, upcoming-events page, one recent + one older event-detail page. `python3 scripts/validate_step6d1_cache.py --cache-dir data/cache/ufcstats` classifies each cached file (`detect_page_type`: completed/upcoming/event_detail/fighter_detail/browser_challenge/unknown), previews parsed counts, flags challenge pages, warns what's missing — offline, never opens the DB. The audit report gained a `cache_source_quality` block (offline_cache_only, cache_dir, cache_files_used, expected_missing, page_types_detected, challenge_pages_detected, parse_warnings, recommendation). No real browser-saved HTML has been supplied yet, so a real diff still awaits those cached pages; DB/processed md5s stay unchanged.
- Step 6E controlled processed-feature rebuild (`ufc_pipeline/step6e_rebuild_processed.py`): after a Step 6D.2 apply changes `data/ufc.db`, regenerates the Step 3/3B/3C processed feature CSVs from the current DB into a SEPARATE dir and verifies them. `python3 scripts/run_step6e_rebuild_processed_from_db.py --db data/ufc.db --output-dir data/processed/rebuild_step6e_<date> --reports-dir reports/data_update/step6e --no-overwrite-official` (optional `--official-step3c`, `--overwrite-rebuild`, `--skip-compat`). It reuses the canonical `features`/`stats_features`/`matchup_features` builders (DB-read-only; Elo read from the snapshots Step 6D.2 inserted, so `build_elo_for_db` is NOT called), brackets the run with a DB md5 before/after assertion, and runs schema parity vs the official Step 3C file (row/column diff, all 43 features present, raw `weight_class` label-only), new-event verification (expected counts present, future-dated cards absent, no duplicate/leakage rows), feature-quality checks, a Step 6B input-validation check, and an optional STRUCTURAL-only official-model compatibility check (reproduces the locked official model from the old official CSV and scores rebuilt rows — no held-out metrics, promotes nothing). Reports go to `reports/data_update/step6e/step6e_rebuild_report.{json,md}`. The non-destructive guard is absolute: never overwrites official `data/processed/` files, the benchmark, or the model; won't clobber a prior rebuild without `--overwrite-rebuild`. Promotion to official is a separate deliberate decision. Latest real run: 8547 -> 8591 rows (Δ +44), max date 2026-06-27, UFC 329 absent, schema identical, all checks green. CAVEAT (learned in Step 6F): the rebuild uses the WORKING-TREE builders, so uncommitted edits to `matchup_features.py`/`stats_features.py` silently change rebuilt feature values; for a data-only rebuild run the builders at their committed versions.
- Step 6F guarded promotion of rebuilt processed files (`ufc_pipeline/step6f_promote_processed.py`): promotes the Step 6E rebuilt Step 3/3B/3C CSVs into the official `data/processed/` paths — a DATA-ARTIFACT promotion only (no retrain/replace/promote of a model, never edits `benchmarks/official_baseline.json`, never touches `data/ufc.db`). `python3 scripts/run_step6f_promote_processed.py --source-dir data/processed/rebuild_step6e_<date> [--apply]` (dry-run default). Guards: requires an explicit `--source-dir`; VALIDATES the source before touching anything (schema parity vs current official Step 3C, expected new events present with correct counts, future cards absent, no duplicate/leakage rows, no infinities, Step 6B input validation — reuses the Step 6E checkers) and aborts with no backup/copy if it fails; on `--apply` copies current official files into `data/processed/backups/pre_step6f_promotion_<UTC-stamp>/` BEFORE any overwrite; re-verifies afterward (each official file byte-equals its source, row count/max date correct, DB+benchmark md5 unchanged) and AUTO-ROLLS-BACK from the backup on post-verify failure. Manifest + rollback instructions -> `reports/data_update/step6f_promotion_report.{json,md}`. Done 2026-07-10: official processed now 8591 rows through 2026-06-27 (committed-formula rebuild; the first rebuild had accidentally used uncommitted matchup-formula edits and was rolled back and redone). DB `c33828a` + benchmark unchanged; frozen benchmark reproduces 0.641920 on the `date <= 2026-05-16` window (the 2 benchmark-reproduction tests slice to that window). 310 tests pass.

## Project Invariants

- Preserve time safety. Elo, record features, recent-form features, and rolling stat features must read historical state before applying the current fight result/stats.
- Sort fights by `(date, fight_id)` when building chronological outputs. `fight_id` preserves source order for same-date tie-breaking.
- Do not let current-fight outcome stats, odds, rankings, post-fight Elo, winner/loser-derived feature names, or precomputed averages become model inputs.
- Keep result/metadata columns such as `winner`, `fighter_a_won`, `method`, and `round` available as labels/metadata, but never use them as pre-fight model features.
- Treat missing historical information as missing/null unless a zero is an exact count, such as prior wins for a debut.
- A partial or null per-fight stats row is not a zero-performance observation and must not enter rolling stat history; only complete rows with exact zero values are usable.
- Model evaluation splits are chronological, never random. Backtest calibration uses a 3-way chronological split (train → calibration → test): the base model fits only on the train window, calibrators (Platt/isotonic) fit only on the calibration window, and reported metrics come only from the final test window. Never fit a calibrator on the final test set.
- Live/rolling calibration (`--mode live`) refits the calibrator on the most recent completed fights (default 730-day window) for FUTURE predictions. Its diagnostics are calibration-window numbers, in-sample for the calibrator — never present them as final-test metrics. Fights after `--as-of-date` must be excluded from training and calibration.
- Platt is the default calibration method; switch to isotonic only when the data clearly proves it better on held-out log loss, and never on a small calibration window.
- Calibration is a separate evaluation layer on top of the Step 3B logistic regression (`ufc_pipeline/calibration.py`); do not replace or overwrite the `compare_models.py` comparison outputs when adding calibrated variants. Live-mode outputs use `live_*` names so backtest outputs are never overwritten.
- Prioritize log loss, Brier score, and calibration gaps when judging probability models; accuracy alone is not a success metric.
- Fighter identity: `fighters.normalized_name` is a matching key, NOT unique — different fighters can share a name. Durable identity lives in `fighter_source_ids` (UFCStats fighter URLs from Greco). Register identities (`build_fighter_identities.py`) before ingesting fights. Never silently merge same-name fighters and never guess ambiguous identities: resolution uses URL first, then unique-name match, then per-bout evidence (exact division weight → career continuity → DOB plausibility); anything still ambiguous is reported as unresolved.
- Never remap existing fighter_id values in place; same-name splits materialize only on a fresh rebuild/re-ingestion.
- Step 3C matchup features follow a strict direction convention: positive value = fighter A advantage. Every matchup feature is arithmetic on PRE-fight rolling snapshots only; any null input makes the feature null (`matchup_history_missing` flags rows where either fighter lacks stat history). Style-archetype scores are deliberately not implemented; do not add them without time-safe era normalization.
- The leakage guard forbids raw fight_stats column names (`td_landed`, `ctrl_seconds`, `knockdowns`, `sub_attempts`) and the older patterns; Step 3B/3C historical names are exact-name allowlisted in `ufc_pipeline/modeling.py` (`STEP3B_MODEL_FEATURES`/`STEP3C_MODEL_FEATURES`).
- Official LR baseline as of 2026-07-07: `step5c_stronger_regularized_lr_drop_weight_class_platt` = Step 3C numeric features, raw `weight_class` dropped, no division interactions, logistic regression with elastic-net (`C=0.003`, `l1_ratio=0.1`, `solver=saga`) + Platt. New benchmark to beat: held-out test re-report log loss 0.641920, Brier 0.225595, AUC 0.679135, accuracy 0.628705. The old official Step 3C LR + Platt is preserved as a historical baseline (log loss 0.6442244532062779, Brier 0.226717, AUC 0.675968, accuracy 0.619345). Step 3B + Platt (0.6449) remains the fallback benchmark; Step 3C calibration outputs use step3c_-prefixed names.
- `benchmarks/official_baseline.json` is the fixed official-benchmark reference. A candidate replaces the official model only by beating the current official benchmark on log loss first (then Brier, then calibration gaps) on the same chronological 3-way split; never regenerate or edit that file without explicit sign-off. The final held-out test was touched in prior diagnostics, so future promotion claims should emphasize validation stability plus consistency with the held-out re-report, not a fresh untouched test.
- Step 5A (`ufc_pipeline/feature_diagnostics.py`) is diagnostics-only: it reproduces the current numeric-only elastic-net official pipeline, audits features/scaling, runs feature-group ablations on the official split, and reports coefficients/permutation importance/leakage checks into `reports/`. It must never change the official model, and log-loss differences under ~0.002 are treated as noise. The original Run 1 findings (former C=1 LR: age/experience most valuable; dropping `weight_class` borderline -0.0026 on test only) are historical context, not current-model results.
- Step 5B (`ufc_pipeline/step5b_regularization_search.py`, Run 2) is an LR-only regularization + `weight_class`-pruning search that never changes the official model. Selection is validation-only: 3 expanding-window rolling-origin folds inside the pre-test period (train+calib), scored by mean Platt log loss; a small pre-declared finalist set (official anchor, best L2/L1/elastic-net, best weight_class configs, best overall) is then scored once on the held-out test. Keep this discipline in any follow-up: never pick hyperparameters/feature sets on the final test window. Run 2 findings: the official anchor reproduced exactly (0.6442244532062779); the best finalist (elastic-net C=0.003, l1_ratio=0.1, all weight_class dropped) hit test log loss 0.6419 (+0.0023, just past the 0.002 threshold) BUT dropping weight_class was not confirmed on pre-test validation (sub-noise ~0.0003) -- the robust lever is stronger regularization than the default C=1.0 (~0.011 validation gain), and L1/elastic-net zero the weight_class one-hots on their own. The official model stayed Step 3C LR + Platt at the end of Run 2.
- Step 5B.1 (`ufc_pipeline/step5b1_regularization_confirmation.py`) confirmed stronger regularization with four pre-declared candidates only and five pre-test folds. Official mean validation Platt log loss was 0.66733; elastic-net C=0.003/l1_ratio=0.1 with `drop_all_weight_class` was 0.65847 (gain 0.00886), beating official in all 5 folds. The already-touched final-test re-report stayed 0.641919 (gain 0.002305), so the edge is promising but not a fresh independent test. Elastic-net all-features (0.65873 validation mean) and the best simple L1 candidate (0.65863) were essentially tied with the drop-all candidate; explicit `weight_class` dropping only improved validation by 0.00026 and elastic-net zeroed all `weight_class` one-hots when included. The official model and official feature set remained unchanged pending Run 3.
- Step 5C (`ufc_pipeline/step5c_division_interactions.py`, Run 3) tested `weight_class × feature` interactions for common/current divisions using five Step 5B.1 pre-test folds and validation-only selection. Eligible divisions were Flyweight, Bantamweight, Featherweight, Lightweight, Welterweight, Middleweight, Light Heavyweight, Heavyweight, Women's Strawweight, Women's Flyweight, and Women's Bantamweight; `Catch Weight`, `Open Weight`, `Super Heavyweight`, and Women's Featherweight were excluded. Interactions beat official but not the stronger no-interaction challenger: validation mean Platt log loss was 0.65847 for EN/drop raw `weight_class`, 0.66166 for the best interaction (`interactions_top_features_only`), 0.66348/0.66351 for full priority interactions without/with raw `weight_class`, and 0.66733 for official. The best interaction lost to the challenger by 0.00319 and won only 1/5 folds vs it. Final-test re-report: challenger 0.641920, best interaction 0.642407, old official 0.644224. Some stable interactions survived (mostly age_diff by division plus a few striking interactions), but the interaction models were worse and more complex; raw `weight_class` remains suspect. Do not promote interactions. Post-Run 3 promotion: the stronger-regularized no-interaction LR (`elasticnet`, C=0.003, l1_ratio=0.1, raw `weight_class` dropped) is now the official LR baseline, and the old Step 3C LR + Platt is historical. Run 4 should compare nonlinear models and ensembles against both the old Step 3C baseline and the new stronger-regularized LR baseline.
- Step 6B (`ufc_pipeline/step6b_live_predictions.py`) is the forward live-tracking layer: it predicts genuinely upcoming fights before they happen, appends to a durable append-only ledger `data/live/live_predictions.csv`, and resolves + scores them once results arrive. It reuses/reproduces the official model exactly (base LR on the official train split, Platt on the official calibration split), validates input (rejects result/target/odds columns; drops raw weight_class), and never retunes/replaces the model, never writes `data/processed/` or the benchmark. The live model report treats the resolved sample as too noisy below 50 and preliminary below 100. Real-model E2E verified (loads step5c_stronger_regularized_lr_drop_weight_class_platt, 5983 train + 1282 calib rows, sensible calibrated probabilities, all leakage checks pass). No scraper yet; future Step 6C could add a data-source updater/scraper (no odds/markets).
- Step 6A (`ufc_pipeline/step6a_pseudo_live_replay.py`) is a pseudo-live replay / pipeline-validation tool, the first step of the live-tracking phase (NOT more model-zoo work). Real run: 360 events / 4,260 fights (UFC 217 2017-11-04 → 2026-05-16; 414 earlier events skipped for insufficient history) with min_train_fights=3000, calibration_fights=1282. Overall pseudo-live log loss 0.6516 (Brier 0.2299, AUC 0.6456, acc 0.6185), +0.0097 above the static backtest 0.6419 — expected, because the replay era is broader and 2017-2019 (~0.667) drags the aggregate while recent years converge to the backtest (2022 0.632, 2025 0.627). Calibration is strong (bucket gaps within ±0.021, high-confidence two-sided gap −0.0068, confidence-bucket accuracy monotonic 0.55→0.86); all 11 leakage/protocol checks pass. It never changes the official model or benchmark and never writes data/processed/. These numbers must not be used to select/retune a model (the final test was already touched). Next phase: a forward live-prediction ledger and/or a data-source updater, NOT another model-zoo search, and no odds/markets unless explicitly approved.
- Step 5D.1 (`ufc_pipeline/step5d1_nonlinear_sanity_audit.py`, Run 4.1) audited the surprising Run 4 result and found NO bug. All anchors reproduced to |Δ|=0.0 (official 0.641920, historical 0.6442244532062779, Run 4 best HGB 0.665388). Probability orientation is correct for LR and HGB (`classes_=[0,1]`, predicted prob = P(fighter A wins); `LL(p) ≪ LL(1−p)`, `AUC(p)≈0.66–0.68` vs `AUC(1−p)≈0.32–0.34`). LR and HGB use identical rows/targets/features across every split/fold (5983/1282/1282, 43 features, identical NaN counts). Calibration is correct (AUC invariant under Platt); HGB's uncalibrated LL (0.681) beats the heavily-regularized LR's raw LL (0.706) but Platt fixes LR to 0.658 vs HGB 0.662, and isotonic is worse for both. HGB handles the 22.5% missing training cells natively (no imputer), which is intended. An expanded staged HGB search (83 configs, ~3.5× Run 4; coarse grid → local refinement + max_iter/early-stopping sub-study, validation-only) found its best in the very shallow (leaf=3) strongly-regularized region at validation LL 0.658343 vs official 0.658466 — a +0.00012 tie, far inside the 0.002 noise band. Early stopping is isolated (internal validation from the fit window; effective n_iter 153 < cap). The Run 4 blend reproduced (validation 0.655548, test 0.643676) but is NOT robust: per-fold optimal weights swing 0.1→0.8 (range 0.70), so the validation edge is fold-noise, matching its final-test failure. Conclusion: the Run 4 result is real — HGB genuinely loses because the signal in these 43 features is essentially linear and a well-regularized LR is already at the frontier. Keep the stronger-regularized LR official, close Run 4, and move to live tracking/monitoring; the official model and `benchmarks/official_baseline.json` are unchanged.
- Step 5D (`ufc_pipeline/step5d_nonlinear_models.py`, Run 4) tested nonlinear models + a blend against the official LR with validation-only selection. Both LR references reproduced the benchmark exactly (anchor test log loss 0.6419199384228512, historical 0.6442244532062779, |Δ|=0.0). The best `HistGradientBoosting` candidate (drop `weight_class`, lr=0.03, leaves=15, min_samples_leaf=100, l2=0.0) lost on validation (mean Platt log loss 0.66164 vs official 0.65847, only 2/5 folds) and lost on the already-touched final test (0.665388 vs 0.641920); it did not improve AUC either (val 0.6230 vs 0.6320; test 0.6606 vs 0.6791), so there was no ranking-vs-calibration trade-off. Isotonic hurt HGB (0.6855 vs Platt 0.6616) — Platt stays the right calibrator; including raw `weight_class` in HGB was sub-noise. The validation-tuned convex blend (w=0.6 LR / 0.4 HGB) beat both on validation (0.65548, +0.00292) but the edge did not survive the final-test re-report (blend test 0.643676 > official 0.641920), so it is not a promotion candidate. Permutation importance (calibration window only; HGB has no native `feature_importances_`) led with `age_diff`/`elo_diff`, overlapping the LR-important set. Conclusion: no nonlinear model or blend cleared the bar (validation edge >0.002 + fold consistency + consistent final-test re-report); keep the stronger-regularized LR official and move to live tracking/monitoring, not a bigger model zoo. The official model and `benchmarks/official_baseline.json` are unchanged. Run 5+ should focus on live tracking; odds/markets remain out unless explicitly approved.

## Fable 5 Orchestrator Workflow

When using Claude Fable 5, act as the orchestrator.

Your job:
- Understand the goal.
- Inspect enough context to form a plan.
- Break the work into clear subtasks.
- Delegate implementation to the `implementer` subagent when edits are needed.
- Delegate review to the `reviewer` subagent after meaningful changes.
- Integrate results into a final answer.
- Keep the main context clean.

Do not personally perform large mechanical edits if a subagent can do them. Use Fable for planning, architecture, risk assessment, debugging strategy, and final synthesis.

Before delegating, give the subagent:
- The exact goal.
- Relevant files or search targets.
- Constraints.
- Definition of done.
- Tests/checks to run.

After subagents return, verify their result before claiming the task is complete.

## Code Style

- Follow the existing small-module style in `ufc_pipeline/`; keep source-specific parsing in adapters or ingestion modules.
- Prefer pandas/SQLite structured operations over ad hoc CSV string manipulation.
- Keep tests focused on time-safety, leakage guards, deterministic ordering, and source conversion edge cases.
- Avoid rewriting generated data files under `data/processed/` unless the task explicitly requires regenerating outputs.
